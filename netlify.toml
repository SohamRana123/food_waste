[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = ".next"

[build.environment]
  PYTHON_VERSION = "3.11.4"

# Next.js on Netlify: handles SSR, rewrites, and .next output correctly.
[[plugins]]
  package = "@netlify/plugin-nextjs"
